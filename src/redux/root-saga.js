import { all } from 'redux-saga/effects';
// import { all, call } from 'redux-saga/effects';

// import sagas

export default function* rootSaga() {
  yield all([
    // call to each single saga
  ]);
}
